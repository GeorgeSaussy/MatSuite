% MATLAB implementation of test 3 to for speed 
lowdim=1000;
hidim=10000;
skip=50;
iter=1000;

for dimiter=lowdim:(hidim-lowdim/skip
    dim=lowdim+skip*dimier;
    H=eye(dim);
    for k=1:dim
        if mod(k,2)==0
            H(k,k)=-1;
        end;
    end;
    v=zero
end;
